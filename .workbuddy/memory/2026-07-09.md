# 2026-07-09

## Bug修复
- **useRoster.ts**: 修复 `ensureDefaultGroup()` 多分组命名不一致问题。当存在多个分组时，第一个分组现在正确命名为"临时名单"而非"临时名单1"，后续分组从"临时名单2"开始。
- **vite.config.ts**: 修复 `renameHtml` 插件无操作Bug。原来的 `renameSync` 源和目标路径相同，现已改为将 `index.html` 重命名为 `Kleros 1.2.html`。同时清理了注释掉的废弃变量。
- **SettingsDialog.vue**: 清理了未使用的 `clearAllDB` 导入。
- **RosterPanel.vue**: 将模板中重复调用4次的 `groupForRosterAllMembers()` 替换为缓存 computed `rosterGroupBindings`，消除了 O(n*m) 的重复计算。同时移除了不再使用的原函数。
- **import.ts / SettingsDialog.vue**: 合并了 `import.ts` 和 `nameParser.ts` 中功能完全相同的 `readFileAsText`/`readFileAsJSON` 函数，统一使用 `nameParser.ts` 中的 `readFileAsText`。
- **TypeScript 类型检查**: `vue-tsc --noEmit` 通过，零错误。
- **版本号 & 更新公告**: 版本号确认为 1.4（vite.config.ts 中已为 `VERSION = '1.4'`），在 `docs/updates/Kleros 1.4.md` 中按既有格式编写了 1.4 更新公告。
